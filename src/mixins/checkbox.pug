mixin checkbox(opts)
    //- Opts object syntax:
    //-
    //- string label: displayed text
    //- string name: form name attribute
    //- bool isChecked: specifies default state (default: false)
    label.checkbox
        if opts.isChecked
            input(type="checkbox" name=opts.name checked)
        else
            input(type="checkbox" name=opts.name)

        .checkbox__label= opts.label
